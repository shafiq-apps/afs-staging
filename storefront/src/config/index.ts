// ============================================================================
// MINIMAL CONSTANTS
// ============================================================================

import { ConfigType } from "../type";

export const Config: ConfigType = {
    DEBOUNCE: 200,
    TIMEOUT: 10000,
    CACHE_TTL: 300000,
    PAGE_SIZE: 24
};